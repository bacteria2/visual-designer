/**
 * Created by lenovo on 2017/6/7.
 */
import ColorPickerList from './src/ColorPickerList.vue';

ColorPickerList.install = function(Vue) {
  Vue.component(ColorPickerList.name, ColorPickerList);
};

export default ColorPickerList;
